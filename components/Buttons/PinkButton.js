

export default function PinkButton({text}) {
  return (
        <button type="submit" 
                className={`drop-shadow-pop bg-pink-600 text-white outline-black outline outline-3 text-lg font-bebas max-w-xs pl-2 pr-2 pt-2 pb-2 m-2`}>
            {text}
        </button>       
        )
    };

  
